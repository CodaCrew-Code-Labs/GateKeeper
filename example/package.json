{
  "name": "@gateway/cognito-auth-example",
  "version": "1.0.0",
  "description": "Example Express application using @gateway/cognito-auth",
  "private": true,
  "main": "index.js",
  "scripts": {
    "start": "node index.js",
    "dev": "node --watch index.js",
    "build-package": "cd .. && npm run build",
    "setup": "npm run build-package && npm install",
    "test-endpoints": "node test-endpoints.js"
  },
  "dependencies": {
    "express": "^4.18.0",
    "dotenv": "^16.0.0"
  },
  "engines": {
    "node": ">=20.0.0"
  }
}